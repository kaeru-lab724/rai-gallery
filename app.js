// Artwork Database (Processed & Categorized by AI)
const artworks = {
  "christmas-night-sky": {
    "title": "クリスマスの夜空に",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "静かなクリスマスの夜、満天の星空をサンタクロースとトナカイたちが駆け抜けていきます。深い藍色と細かな星屑が織りなす夜空の質感が、温かみのある光を優しく引き立てています。",
    "image": "assets/images/24年、クリスマスの夜空に.webp",
    "auraColor": "rgba(26, 54, 110, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "ohanami-picnic": {
    "title": "お花見ピクニック",
    "meta": "2026 | 水彩、紙 | 297 x 210 mm",
    "desc": "満開の桜の下で、仲良くお花見を楽しむ二匹の猫たちの姿が描かれています。柔らかなピンクと緑の色彩が、春のうららかな陽気とお弁当を囲む優しい時間を伝えてくれます。",
    "image": "assets/images/26_お花見ピクニック.webp",
    "auraColor": "rgba(255, 209, 220, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "cat-day": {
    "title": "猫の日",
    "meta": "2026 | 水彩、紙 | 210 x 297 mm",
    "desc": "澄んだ青い瞳をした美しいグレーの猫が、静かにたたずんでいます。水彩の重なりによって表現された柔らかな毛並みと、神秘的な眼差しが心を引き込みます。",
    "image": "assets/images/26_猫の日.webp",
    "auraColor": "rgba(135, 150, 190, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "52hz-whale": {
    "title": "52Hzのクジラ",
    "meta": "2024 | ペン、紙 | 210 x 297 mm",
    "desc": "世界で最も孤独とされる「52ヘルツのクジラ」を描いた細密なペン画。満天の星々と月が輝く静寂の海で、誰にも届かない歌を歌うクジラの姿がどこか優しく、切なく表現されています。",
    "image": "assets/images/52Hzのクジラ＿ペン.webp",
    "auraColor": "rgba(50, 50, 60, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "goodnight-kitty": {
    "title": "おやすみねこちゃん",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "枕元に置かれた本と温かなテーブルランプの横で、すやすやと眠るねこちゃん。青から茶へと移り変わる背景が、夜の静けさと部屋のぬくもりを優しく包み込んでいます。",
    "image": "assets/images/おやすみねこちゃん.webp",
    "auraColor": "rgba(92, 115, 140, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "sleeping-calico-cat": {
    "title": "お昼寝三毛猫",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "白いクッションに体を丸めて心地よさそうに眠る三毛猫。暖色系の水彩タッチが、ぽかぽかとした陽だまりの温かさと穏やかなお昼寝の時間を表現しています。",
    "image": "assets/images/お昼寝三毛猫.webp",
    "auraColor": "rgba(184, 91, 53, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "polar-bear-cookies": {
    "title": "しろくまさんのクッキー作り",
    "meta": "2025 | 水彩、紙 | 297 x 210 mm",
    "desc": "緑のオーバーオールを着たしろくまさんが、楽しそうにクリスマスのアイシングクッキーを作っています。背景のあたたかいピンクとオレンジのグラデーションが、手作りのぬくもりを引き立てています。",
    "image": "assets/images/しろくまさんのクッキー作り.webp",
    "auraColor": "rgba(240, 168, 150, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "tanuki-to-kitsune": {
    "title": "たぬきときつね",
    "meta": "2023 | 水彩、紙 | 297 x 210 mm",
    "desc": "秋の森で、どんぐりや落ち葉に囲まれて向かい合うたぬきときつね。「おそろいだね！」と話しかけるような二匹の温かい交流が、優しい黄色とオレンジの水彩で描かれています。",
    "image": "assets/images/たぬきときつね.webp",
    "auraColor": "rgba(240, 204, 127, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "neko-autumn-plants-black-bg": {
    "title": "秋の植物と黒い背景のねこ",
    "meta": "2023 | 水彩、紙 | 297 x 210 mm",
    "desc": "暗闇のなかから浮かび上がるように描かれた、美しい緑の瞳を持つ長毛の猫。周囲を彩るキンモクセイやイチョウの鮮やかな秋の色彩が、漆黒の背景とのコントラストで幻想的に輝いています。",
    "image": "assets/images/ねこ＿秋の植物と黒い背景.webp",
    "auraColor": "rgba(60, 60, 60, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "easter-and-rabbit": {
    "title": "イースターとうさぎ",
    "meta": "2026 | 水彩、紙 | 297 x 210 mm",
    "desc": "カラフルなイースターエッグや春を告げるパンジー、チューリップに囲まれて、嬉しそうに転がるうさぎさん。明るく軽やかな色彩が、春の訪れを祝う楽しげなメロディのように響きわたります。",
    "image": "assets/images/イースターとうさぎ＿26.webp",
    "auraColor": "rgba(163, 222, 185, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "lobo-the-king-of-currumpaw": {
    "title": "オオカミ王ロボ",
    "meta": "2024 | ペン、紙 | 210 x 297 mm",
    "desc": "『シートン動物記』の狼王ロボをテーマにしたペン画。眠るロボの影の中に、星空と愛するブランカの姿が浮かび上がる構成が、切なくも美しい物語を紡ぎ出しています。",
    "image": "assets/images/オオカミ王ロボ＿ペン.webp",
    "auraColor": "rgba(40, 40, 50, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "cushions-and-retriever": {
    "title": "クッションとレトリバー",
    "meta": "2023 | 水彩、紙 | 297 x 210 mm",
    "desc": "カラフルなクッションに囲まれて、気持ちよさそうに眠りにつくゴールデンレトリバー。柔らかな黄と緑の色彩が、愛犬の安らかな寝顔と温かいリビングの雰囲気を伝えています。",
    "image": "assets/images/クッションとレトリバー.webp",
    "auraColor": "rgba(235, 190, 110, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "cosmos-and-collie": {
    "title": "コスモスとコリー",
    "meta": "2025 | 水彩、紙 | 297 x 210 mm",
    "desc": "秋風に揺れるピンクのコスモス畑で、静かにあごをのせて休むコリー犬。切なげで優しい眼差しと、温かみのある茶色い毛並みが水彩のソフトな濃淡で繊細に描かれています。",
    "image": "assets/images/コスモスとコリー.webp",
    "auraColor": "rgba(214, 114, 90, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "santa-rabbit": {
    "title": "サンタのうさぎ",
    "meta": "2023 | 水彩、紙 | 210 x 210 mm",
    "desc": "サンタクロースの赤い衣装と白いヒゲを身にまとった、お茶目なうさぎさん。夜空を見上げてクリスマスの鐘の音を待つような姿が、鮮やかな赤とソフトな水彩表現で愛らしく描かれています。",
    "image": "assets/images/サンタのうさぎ.webp",
    "auraColor": "rgba(224, 76, 91, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "shepherd": {
    "title": "シェパード",
    "meta": "2023 | 水彩、紙 | 210 x 210 mm",
    "desc": "りりしくも穏やかな表情を見せるジャーマン・シェパード・ドッグ。茶と黒の力強い色彩のなかに、水彩ならではの透明感と温かみが宿り、忠実で賢い犬の魅力を引き立てています。",
    "image": "assets/images/シェパード＿23,9,4.webp",
    "auraColor": "rgba(196, 120, 84, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "soda-yogurt-float": {
    "title": "ソーダヨーグルトフロート",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "涼しげなガラスコップに注がれた、爽やかな青いソーダフロート。アイスクリームの上にかかった青いシロップと、透き通るブルーのグラデーションが夏らしさを運んでくれます。",
    "image": "assets/images/ソーダヨーグルトフロート.webp",
    "auraColor": "rgba(74, 144, 226, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "tiger-pen": {
    "title": "トラ",
    "meta": "2025 | ペン・色鉛筆、紙 | 297 x 210 mm",
    "desc": "横顔を見せるトラ의 細密なペン画に、色鉛筆の柔らかな橙色が重ねられています。瞳の先できらきらと輝く金色の光の粒が、野性的な力強さの中にファンタジーの香りを漂わせています。",
    "image": "assets/images/トラ＿ペン.webp",
    "auraColor": "rgba(232, 140, 74, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "herb-tea-and-white-fox": {
    "title": "ハーブティーとしろきつね",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "大きなハーブティーのカップに寄り添い、気持ちよさそうに目を閉じる真っ白なしろきつね。立ち上る湯気とカップの中の花々が、心まで温まるようなホッとする安らぎの時間を運んでくれます。",
    "image": "assets/images/ハーブティーとしろきつね.webp",
    "auraColor": "rgba(245, 230, 200, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "picnic-and-rabbit": {
    "title": "ピクニックとうさぎ",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "緑のスカートと白いブラウスを着て、小さなピクニックバスケットを持った愛らしうさぎさん。背景を彩る野の花やブルーベリーが、春のピクニックのわくわくする雰囲気を優しく引き立てています。",
    "image": "assets/images/ピクニックとうさぎ.webp",
    "auraColor": "rgba(144, 210, 160, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "ferret-mailman": {
    "title": "フェレットの郵便屋さん",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "青いバラの花と大切な手紙を抱え、誇らしげに微笑むフェレットの郵便屋さん。涼しげなミントブルーの水彩と紫のリボンが、心を届ける特別な瞬間のワクワク感を美しく彩っています。",
    "image": "assets/images/フェレットの郵便屋さん.webp",
    "auraColor": "rgba(132, 212, 219, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "owl": {
    "title": "フクロウ",
    "meta": "2025 | 水彩、紙 | 297 x 210 mm",
    "desc": "空いっぱいに両翼を広げて飛翔する美しいフクロウ。淡いブルーの影が羽の白さを引き立て、周囲に散りばめられた小さな色彩のしずくが、静かな風を感じさせてくれます。",
    "image": "assets/images/フクロウ.webp",
    "auraColor": "rgba(200, 220, 240, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "lilac-and-dog": {
    "title": "ライラックとわんこ",
    "meta": "2026 | 水彩、紙 | 297 x 210 mm",
    "desc": "青い瞳を輝かせ、嬉しそうに口を開けて微笑む可愛らしいわんこ。舞い散るライラックの紫やピンクの花びらが、うららかな春の陽だまりのような幸せな空気を演出しています。",
    "image": "assets/images/ライラックとわんこ＿26,5,9_0002.webp",
    "auraColor": "rgba(230, 210, 240, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "ragdoll-and-spring-flowers": {
    "title": "ラグドールと春の花々",
    "meta": "2025 | 水彩、紙 | 297 x 210 mm",
    "desc": "カラフルなチューリップやオレンジのお花に寄り添い、ゴロンと仰向けになって見つめるラグドール。透き通るような青い瞳とふわふわとした毛並みが、春を喜ぶ無邪気な愛らしさを伝えています。",
    "image": "assets/images/ラグドールと春の花々.webp",
    "auraColor": "rgba(255, 235, 180, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "playground-of-squirrels": {
    "title": "リス達の遊び場所",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "吊り下げられた美しい青い陶器の植木鉢の中で、嬉しそうに遊ぶ二匹のリス。背景に広がる爽やかなミントグリーンと赤や青の色彩が、木漏れ日あふれる庭先の穏やかな一日を感じさせます。",
    "image": "assets/images/リス達の遊び場所.webp",
    "auraColor": "rgba(168, 218, 220, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "lemon-and-polar-bear-cub": {
    "title": "レモンとしろくまのこ",
    "meta": "2025 | 水彩、紙 | 210 x 297 mm",
    "desc": "ぬいぐるみのような可愛らしいしろくまの子が、鮮やかな黄色のレモンを大切そうに抱えています。首に巻いたボーダーのリボンと背景の爽やかな水色が、初夏のような清々しさを感じさせてくれます。",
    "image": "assets/images/レモンとしろくまのこ_0002.webp",
    "auraColor": "rgba(135, 206, 235, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "lemon-and-polar-bear-cub-wink": {
    "title": "レモンとしろくまのこ（ウインク）",
    "meta": "2025 | 水彩、紙 | 210 x 210 mm",
    "desc": "左目をきゅっと閉じてウインクをする、愛らしいしろくまの子のカット。首元に飾られたボーダーのリボンと、レモンをモチーフにしたブローチがとってもおしゃれでチャーミングです。",
    "image": "assets/images/レモンとしろくまのこ＿ウインク_0002.webp",
    "auraColor": "rgba(255, 223, 0, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "eagle-owl": {
    "title": "ワシミミズク",
    "meta": "2025 | ペン・水彩、紙 | 210 x 297 mm",
    "desc": "枝の上にまっすぐ佇み、大きな丸い瞳でこちらを見つめるワシミミズク。細密なペン画に重なるブラウンやディープブルーの水彩が、知性と野生味を兼ね備えた神秘的な姿を描き出しています。",
    "image": "assets/images/ワシミミズク25,10,31.webp",
    "auraColor": "rgba(90, 80, 75, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "calico-cat-and-polar-bear": {
    "title": "三毛猫としろくま",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "大きなしろくまと小さな三毛猫のコミカルで温かい日常を描いたイラスト。お天気を気にしてテレビを見たり、頭の上に乗って会話したりする二匹の姿が、優しいパステルカラーで描かれています。",
    "image": "assets/images/三毛猫としろくま.webp",
    "auraColor": "rgba(255, 245, 220, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "dove-and-olive": {
    "title": "鳩とオリーブ",
    "meta": "2025 | ペン、紙 | 210 x 297 mm",
    "desc": "旧約聖書のノアの箱舟の物語をモチーフにした、細密なペン画。「報せを運んだ しろいきみ」の言葉とともに、オリーブの枝をくわえて星空を飛ぶ白い鳩の姿が、厳かで希望に満ちた雰囲気で描かれています。",
    "image": "assets/images/世界一有名なお話＿鳩とオリーブ.webp",
    "auraColor": "rgba(60, 60, 60, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "remains-of-summer": {
    "title": "夏の残り物",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "ラムネの瓶と、その中で静かに火花を散らす線香花火を見つめる猫。金魚が宙を舞う幻想的な表現と涼しげなブルーが、夏の終わりの少し切なくも美しい余韻を感じさせてくれます。",
    "image": "assets/images/夏の残り物.webp",
    "auraColor": "rgba(90, 160, 220, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "twilight-and-cat": {
    "title": "夕暮れと猫",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "格子窓から静かに外を見つめ、夕暮れの空を眺める猫の横顔。オレンジから紫、青へと移り変わる夕空のグラデーションが、一日の終わりの穏やかな静寂を美しく演出しています。",
    "image": "assets/images/夕暮れと猫.webp",
    "auraColor": "rgba(180, 140, 180, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "felicette-space-cat": {
    "title": "宇宙を旅した猫",
    "meta": "2024 | ペン、紙 | 210 x 297 mm",
    "desc": "1963年に宇宙へ行った実在の猫「フェリセット」をオマージュした細密なペン画。ロケットを背景に、無重力空間でふわりと浮かびながら無数の星々と出会う姿が神秘的に描かれています。",
    "image": "assets/images/宇宙を旅した猫＿フェリセット.webp",
    "auraColor": "rgba(40, 40, 50, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "whale-swimming-spring-to-summer": {
    "title": "春から夏へ泳ぐ鯨",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "季節の移ろいの中をのびのびと泳ぐ、穏やかな表情をした大きな青いクジラ。尾ひれのまわりに舞うピンクの桜吹雪と、目の前を流れる新緑のグリーンが、うつくしい季節の境界線を描き出しています。",
    "image": "assets/images/春から夏へ泳ぐ鯨.webp",
    "auraColor": "rgba(90, 140, 210, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "profile-cat": {
    "title": "横向きの猫",
    "meta": "2023 | 水彩、紙 | 297 x 210 mm",
    "desc": "美しい横顔を見せる長毛の猫。水彩の淡いレインボーカラーが横向きの猫に重なり、周囲に描かれた繊細なアラベスク模様のような草花のラインが、夢の中のような優しい世界観を醸し出しています。",
    "image": "assets/images/横向きの猫.webp",
    "auraColor": "rgba(245, 220, 180, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "new-year-dragon": {
    "title": "正月の竜",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "新春を祝うお正方に描かれた、瑞獣である美しい白龍。金色の梅の花や雲の和風モチーフに囲まれ、虹色に輝くたてがみをなびかせて優雅に舞う姿が描かれています。",
    "image": "assets/images/正月の竜.webp",
    "auraColor": "rgba(255, 225, 150, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "preparation-start-over": {
    "title": "準備はやり直し",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "クリスマスの飾り付け中に、赤いリボンに絡まって遊んでしまう白黒の猫ちゃん。オーナメントや金色の星に囲まれ、「準備はやり直し」とお茶目に困るような温かいクリスマスの情景です。",
    "image": "assets/images/準備はやり直し.webp",
    "auraColor": "rgba(180, 110, 80, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "white-cat-blue-eyes-1006": {
    "title": "青い瞳の白猫",
    "meta": "2025 | ペン・水彩、紙 | 210 x 297 mm",
    "desc": "澄んだブルーの瞳が印象的な長毛の白猫。細やかなペン画のラインに重ねられた淡い水色のシャドウが、清潔感としなやかな美しさを際立たせています。",
    "image": "assets/images/猫25,10,6.webp",
    "auraColor": "rgba(200, 220, 245, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "green-eyed-cat-1007": {
    "title": "緑の瞳の猫",
    "meta": "2025 | ペン・水彩、紙 | 210 x 297 mm",
    "desc": "鮮やかで透き通るような緑の瞳をした猫が、こちらを静かに見つめています。淡いグリーンの水彩画が、瑞々しく穏やかな森の木漏れ日のような癒やしを与えてくれます。",
    "image": "assets/images/猫25,10,7.webp",
    "auraColor": "rgba(140, 215, 180, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "coffee-day": {
    "title": "珈琲の日",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "10月1日の「コーヒーの日」をテーマにしたイラスト。コーヒーカップにすっぽりと収まった茶色い猫の後頭部には、ハートを描いた美しいラテアートの模様があしらわれており、ユーモアと温もりにあふれています。",
    "image": "assets/images/珈琲の日.webp",
    "auraColor": "rgba(100, 70, 50, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "white-dragon-and-metallic-ornament": {
    "title": "白龍と金属飾り",
    "meta": "2023 | 水彩、紙 | 297 x 210 mm",
    "desc": "精巧な金色の金属工芸の花々や青い宝玉の上に、翼を広げてたたずむ神秘的な白龍。白龍の羽や鱗に施された淡いブルーとイエローのグラデーションが、高貴で繊細な美しさを放っています。",
    "image": "assets/images/白龍と金属飾り.webp",
    "auraColor": "rgba(240, 220, 160, 0.4)",
    "zone": "stars",
    "suzuriUrl": "#"
  },
  "polar-bear-and-cat-watching-sky": {
    "title": "空鑑賞のしろくまと猫",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "白く大きなクマが小さな三毛猫を腕に抱き、窓から広がる美しい夜空を静かに見つめています。深い紺色と紫が混ざり合う宇宙のような窓の外と、二匹を包み込む優しい温もりが対照的で神秘的です。",
    "image": "assets/images/空鑑賞のしろくまと猫.webp",
    "auraColor": "rgba(90, 110, 180, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "green-eyed-fluffy-cat": {
    "title": "緑目の猫",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "豊かな毛並みと神秘的な緑色の瞳を持った赤茶色の猫。水彩の温かいオレンジブラウンのグラデーションと、周囲に散る爽やかな青や黄色のしずくが、鮮やかで優しいコントラストを描いています。",
    "image": "assets/images/緑目の猫.webp",
    "auraColor": "rgba(210, 110, 70, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "sailing-ship": {
    "title": "船",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "荒れ狂う深い青と紫の海原を、力強く進む木造の帆船。波しぶきや光のきらめきがダイナミックな水彩のタッチで描かれ、冒険への旅立ちを予感させるドラマチックな一枚です。",
    "image": "assets/images/船.webp",
    "auraColor": "rgba(26, 54, 110, 0.4)",
    "zone": "water",
    "suzuriUrl": "#"
  },
  "flowers-and-retriever": {
    "title": "花とレトリバー",
    "meta": "2025 | 水彩、紙 | 210 x 297 mm",
    "desc": "チューリップなどのお花を嬉しそうにくわえ、満面の笑みを浮かべるゴールデンレトリバー。優しい色彩の重なりと、愛犬 of 心からの喜びが伝わってくるような温かな表情が胸を打ちます。",
    "image": "assets/images/花とレトリバー.webp",
    "auraColor": "rgba(255, 230, 180, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  },
  "bouquet-of-roses": {
    "title": "薔薇の花束",
    "meta": "2024 | 水彩、紙 | 297 x 210 mm",
    "desc": "赤、青、黄、白などカラフルな薔薇の花々が飾られ、ヴィンテージのタグと指輪があしらわれ、深みのある夜空のような青い背景が、静かでロマンチックな物語を想起させます。",
    "image": "assets/images/薔薇の花束.webp",
    "auraColor": "rgba(90, 100, 160, 0.4)",
    "zone": "forest",
    "suzuriUrl": "#"
  }
};

const artworkKeys = Object.keys(artworks);
let currentArtworkKey = '';

// DOM Elements
const entrance = document.getElementById('entrance');
const btnEnter = document.getElementById('btn-enter');
const galleryContainer = document.getElementById('gallery-container');
const galleryTrack = document.getElementById('gallery-track');
const uiControls = document.getElementById('ui-controls');
const spotlight = document.getElementById('spotlight');
const btnTheme = document.getElementById('btn-theme');
const btnAudio = document.getElementById('btn-audio');

// Lightbox Elements
const lightbox = document.getElementById('lightbox');
const lightboxImg = document.getElementById('lightbox-img');
const lightboxTitle = document.getElementById('lightbox-title');
const lightboxMeta = document.getElementById('lightbox-meta');
const lightboxDesc = document.getElementById('lightbox-desc');
const linkGoods = document.getElementById('link-goods');
const btnShare = document.getElementById('btn-share');
const btnPrev = document.getElementById('btn-prev');
const btnNext = document.getElementById('btn-next');
const lightboxAura = document.getElementById('lightbox-aura');
const bleedDisplacement = document.getElementById('bleed-displacement');

let bleedAnimId = null;

// Audio Node References
let audioCtx = null;
let synthNodes = [];
let isPlaying = false;
let mainFilterNode = null;
let starChimeGain = null;
let starChimeInterval = null;

// Room transition tracking flags
let currentZone = 'water';
let isTransitioning = false;
const zoneLimits = {
    'water': 0,
    'forest': 0,
    'stars': 0
};

// Dynamic Gallery Generation
function generateGallery() {
    const track = document.getElementById('gallery-track');
    if (!track) return;
    
    // Select the end marker (Artist profile wall)
    const artistWall = track.querySelector('.artist-wall');
    if (!artistWall) return;
    
    // Clean dynamic walls if exist
    const oldWalls = track.querySelectorAll('.dynamic-wall');
    oldWalls.forEach(w => w.remove());
    
    // Sort keys based on zone order: water -> forest -> stars
    const sortedKeys = Object.keys(artworks).sort((a, b) => {
        const zones = { 'water': 1, 'forest': 2, 'stars': 3 };
        return zones[artworks[a].zone] - zones[artworks[b].zone];
    });
    
    // Synchronize artwork keys list for lightbox sliding navigation
    artworkKeys.length = 0;
    artworkKeys.push(...sortedKeys);
    
    // Group artworks in pairs to build double-hanging walls
    const grouped = [];
    let currentGroup = [];
    let activeZone = '';
    
    sortedKeys.forEach(key => {
        const data = artworks[key];
        if (activeZone !== data.zone) {
            // Push remaining from previous zone
            if (currentGroup.length > 0) {
                grouped.push({ zone: activeZone, items: currentGroup });
                currentGroup = [];
            }
            activeZone = data.zone;
        }
        currentGroup.push(key);
        // Pair structure: max 2 items per wall
        if (currentGroup.length === 2) {
            grouped.push({ zone: activeZone, items: currentGroup });
            currentGroup = [];
        }
    });
    // Push the very last remaining item
    if (currentGroup.length > 0) {
        grouped.push({ zone: activeZone, items: currentGroup });
    }
    
    let index = 1;
    let wallIndex = 0;
    grouped.forEach(group => {
        wallIndex++;
        const isReverse = (wallIndex % 2 === 0);
        
        const section = document.createElement('section');
        section.className = 'gallery-section artwork-wall dynamic-wall';
        section.setAttribute('data-zone', group.zone);
        
        // Define zone color mappings for dynamic backdrop interpolation
        const bgColors = {
            'water': { night: '#090d16', day: '#d9e2ec' },
            'forest': { night: '#0f1711', day: '#dfe5e0' },
            'stars': { night: '#120c18', day: '#eae4e9' }
        };
        const colors = bgColors[group.zone] || bgColors['forest'];
        section.setAttribute('data-bg-night', colors.night);
        section.setAttribute('data-bg-day', colors.day);
        
        let htmlA = '';
        let htmlB = '';
        
        // Item 1: Midground (smaller, background)
        if (group.items[0]) {
            const keyA = group.items[0];
            const dataA = artworks[keyA];
            const wallNumA = String(index++).padStart(2, '0');
            const offsetA = isReverse ? 40 : -40; // Zig-zag offsets
            
            htmlA = `
                <div class="parallax-layer mid-layer" style="--y-offset: ${offsetA}px;">
                    <div class="artwork-container">
                        <span class="wall-number">${wallNumA}</span>
                        <div class="frame frame-wooden clickable-frame" onclick="openLightbox('${keyA}')">
                            <div class="artwork-wrapper">
                                <img src="${dataA.image}" alt="${dataA.title}" class="gallery-image" loading="lazy">
                                <div class="glass-reflection"></div>
                            </div>
                        </div>
                        <div class="museum-plaque">
                            <h4 class="artwork-title">${dataA.title}</h4>
                            <p class="artwork-meta">${dataA.meta}</p>
                        </div>
                    </div>
                </div>
            `;
        }
        
        // Item 2: Foreground (larger, foreground)
        if (group.items[1]) {
            const keyB = group.items[1];
            const dataB = artworks[keyB];
            const wallNumB = String(index++).padStart(2, '0');
            const offsetB = isReverse ? -50 : 50; // Opposite offsets
            const frameStyles = ['frame-gold', 'frame-dark'];
            const frameClass = frameStyles[index % frameStyles.length];
            
            htmlB = `
                <div class="parallax-layer fore-layer" style="--y-offset: ${offsetB}px;">
                    <div class="artwork-container">
                        <span class="wall-number">${wallNumB}</span>
                        <div class="frame ${frameClass} clickable-frame" onclick="openLightbox('${keyB}')">
                            <div class="artwork-wrapper">
                                <img src="${dataB.image}" alt="${dataB.title}" class="gallery-image" loading="lazy">
                                <div class="glass-reflection"></div>
                            </div>
                        </div>
                        <div class="museum-plaque">
                            <h4 class="artwork-title">${dataB.title}</h4>
                            <p class="artwork-meta">${dataB.meta}</p>
                        </div>
                    </div>
                </div>
            `;
        }
        
        // Swap flex rendering order alternatively to create a dynamic layout flow
        section.innerHTML = isReverse ? (htmlB + htmlA) : (htmlA + htmlB);
        track.insertBefore(section, artistWall);
    });
    
    // Update shop numbers to be dynamic after list
    const shopWall = track.querySelector('.shop-wall');
    if (shopWall) {
        const shopWallNum = shopWall.querySelector('.wall-number');
        if (shopWallNum) {
            shopWallNum.textContent = String(index + 1).padStart(2, '0');
        }
    }
    const artistWallNum = artistWall.querySelector('.wall-number');
    if (artistWallNum) {
        artistWallNum.textContent = String(index).padStart(2, '0');
    }
}

// 1. Entrance Handler
btnEnter.addEventListener('click', () => {
    entrance.classList.add('fade-out');
    galleryContainer.classList.remove('hide');
    uiControls.classList.remove('hide');
    
    setTimeout(() => {
        entrance.style.display = 'none';
        galleryContainer.classList.add('show');
        uiControls.classList.add('show');
        
        // Initial backdrop colors and sound state alignment
        updateBackdropAndSound();
        calculateZoneBoundaries();
    }, 1200);
});

// Calculate pixel scroll boundaries for room transitions
function calculateZoneBoundaries() {
    const walls = Array.from(document.querySelectorAll('.artwork-wall'));
    if (walls.length === 0) return;
    
    let lastWaterIdx = -1;
    let lastForestIdx = -1;
    
    walls.forEach((wall, idx) => {
        const zone = wall.getAttribute('data-zone');
        if (zone === 'water') lastWaterIdx = idx;
        if (zone === 'forest') lastForestIdx = idx;
    });
    
    const containerWidth = galleryContainer.clientWidth;
    
    if (lastWaterIdx !== -1 && walls[lastWaterIdx]) {
        zoneLimits['water'] = walls[lastWaterIdx].offsetLeft + walls[lastWaterIdx].offsetWidth - containerWidth / 2;
    }
    if (lastForestIdx !== -1 && walls[lastForestIdx]) {
        zoneLimits['forest'] = walls[lastForestIdx].offsetLeft + walls[lastForestIdx].offsetWidth - containerWidth / 2;
    }
}

// 2. Horizontal Scrolling (converts vertical wheel scrolling to horizontal scroll)
galleryContainer.addEventListener('wheel', (e) => {
    if (window.innerWidth > 768 && !isTransitioning) {
        e.preventDefault();
        galleryContainer.scrollBy({
            left: e.deltaY * 1.5,
            behavior: 'auto'
        });
    }
}, { passive: false });

// 3. Spotlight Cursor Tracker & Gyroscope setup
document.addEventListener('mousemove', (e) => {
    const x = (e.clientX / window.innerWidth) * 100;
    const y = (e.clientY / window.innerHeight) * 100;
    
    document.documentElement.style.setProperty('--mouse-x', `${x}%`);
    document.documentElement.style.setProperty('--mouse-y', `${y}%`);
});

// Handle mobile device tilt (Gyroscope)
let lastBeta = 0;
let lastGamma = 0;
window.addEventListener('deviceorientation', (e) => {
    const beta = e.beta; 
    const gamma = e.gamma;
    
    if (beta === null || gamma === null) return;
    
    // Smooth using simple LERP filter
    const smoothBeta = lastBeta + (beta - lastBeta) * 0.15;
    const smoothGamma = lastGamma + (gamma - lastGamma) * 0.15;
    
    lastBeta = smoothBeta;
    lastGamma = smoothGamma;
    
    // Map to offset pixels (-30px to 30px)
    const gyroX = Math.max(-30, Math.min(30, smoothGamma * 0.7));
    // Assume user holds phone tilted at 50 degrees naturally
    const gyroY = Math.max(-30, Math.min(30, (smoothBeta - 50) * 0.7));
    
    document.documentElement.style.setProperty('--gyro-x', `${gyroX}px`);
    document.documentElement.style.setProperty('--gyro-y', `${gyroY}px`);
});

// 4. Day / Night Theme Toggler
btnTheme.addEventListener('click', () => {
    const body = document.body;
    if (body.classList.contains('theme-night')) {
        body.classList.remove('theme-night');
        body.classList.add('theme-day');
        btnTheme.innerHTML = `<i class="fa-solid fa-sun"></i><span class="btn-label">Day Mode</span>`;
    } else {
        body.classList.remove('theme-day');
        body.classList.add('theme-night');
        btnTheme.innerHTML = `<i class="fa-solid fa-moon"></i><span class="btn-label">Night Mode</span>`;
    }
    
    // Recalculate backdrop blending for the active theme
    updateBackdropAndSound();
});

// 5. Ambient sound generator (Web Audio API)
function initAmbientSynth() {
    audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    
    // Low pass filter for soft/warm default tones
    mainFilterNode = audioCtx.createBiquadFilter();
    mainFilterNode.type = 'lowpass';
    mainFilterNode.frequency.setValueAtTime(220, audioCtx.currentTime); // start deep/muffled
    mainFilterNode.Q.setValueAtTime(1.2, audioCtx.currentTime);

    // Master volume
    const masterGain = audioCtx.createGain();
    masterGain.gain.setValueAtTime(0.0, audioCtx.currentTime); // start silent

    // Frequencies representing C major 9 chord (C3, G3, B3, D4, G4)
    const freqs = [130.81, 196.00, 246.94, 293.66, 392.00];
    
    // Create oscillators
    freqs.forEach((freq, idx) => {
        const osc = audioCtx.createOscillator();
        const oscGain = audioCtx.createGain();
        
        osc.type = 'sine';
        osc.frequency.setValueAtTime(freq, audioCtx.currentTime);
        
        // Slight detune for chorus warmth
        osc.detune.setValueAtTime((Math.random() - 0.5) * 8, audioCtx.currentTime);
        
        // Individual gains
        oscGain.gain.setValueAtTime(0.06 / freqs.length, audioCtx.currentTime);
        
        // Slow volume LFO to make the chord breathe
        const lfo = audioCtx.createOscillator();
        const lfoGain = audioCtx.createGain();
        lfo.frequency.setValueAtTime(0.05 + idx * 0.02, audioCtx.currentTime); // very slow
        lfoGain.gain.setValueAtTime(0.02, audioCtx.currentTime);
        
        lfo.connect(lfoGain);
        lfoGain.connect(oscGain.gain);
        
        osc.connect(oscGain);
        oscGain.connect(mainFilterNode);
        
        lfo.start();
        osc.start();
        
        synthNodes.push(osc, lfo);
    });
    
    // Starry night high chime generator gain node
    starChimeGain = audioCtx.createGain();
    starChimeGain.gain.setValueAtTime(0.0, audioCtx.currentTime); // initially silent in water zone
    starChimeGain.connect(audioCtx.destination);
    
    mainFilterNode.connect(masterGain);
    masterGain.connect(audioCtx.destination);
    
    // Start background random chime generation
    startStarChimeLoop();
    
    return masterGain;
}

let masterGainNode = null;

btnAudio.addEventListener('click', () => {
    if (!audioCtx) {
        masterGainNode = initAmbientSynth();
    }
    
    if (audioCtx.state === 'suspended') {
        audioCtx.resume();
    }
    
    if (!isPlaying) {
        // Fade sound in
        masterGainNode.gain.linearRampToValueAtTime(0.4, audioCtx.currentTime + 2.0);
        btnAudio.innerHTML = `<i class="fa-solid fa-volume-high"></i><span class="btn-label">Sound On</span>`;
        isPlaying = true;
        
        // Immediately sync parameters to current scroll position
        setTimeout(() => {
            if (galleryContainer) {
                const scrollLeft = galleryContainer.scrollLeft;
                const maxScroll = galleryContainer.scrollWidth - galleryContainer.clientWidth;
                updateAmbientSynth(scrollLeft, maxScroll);
            }
        }, 100);
    } else {
        // Fade sound out
        masterGainNode.gain.linearRampToValueAtTime(0.0, audioCtx.currentTime + 1.0);
        btnAudio.innerHTML = `<i class="fa-solid fa-volume-xmark"></i><span class="btn-label">Sound Off</span>`;
        isPlaying = false;
        
        if (starChimeGain) {
            starChimeGain.gain.setValueAtTime(0, audioCtx.currentTime);
        }
    }
});

// 6. Watercolor Bleed Effect Animation
function triggerBleedEffect() {
    if (bleedAnimId) {
        cancelAnimationFrame(bleedAnimId);
    }
    
    const duration = 1500;
    const startScale = 250;
    const startTime = performance.now();
    
    function animate(currentTime) {
        const elapsed = currentTime - startTime;
        const progress = Math.min(elapsed / duration, 1);
        const easeOutQuart = 1 - Math.pow(1 - progress, 4);
        const currentScale = startScale * (1 - easeOutQuart);
        
        if (bleedDisplacement) {
            bleedDisplacement.setAttribute('scale', currentScale);
        }
        
        if (progress < 1) {
            bleedAnimId = requestAnimationFrame(animate);
        } else {
            bleedAnimId = null;
        }
    }
    
    bleedAnimId = requestAnimationFrame(animate);
}

// 7. Lightbox logic
function openLightbox(key) {
    currentArtworkKey = key;
    const data = artworks[key];
    if (!data) return;
    
    lightboxImg.src = data.image;
    lightboxImg.alt = data.title;
    lightboxTitle.textContent = data.title;
    lightboxMeta.textContent = data.meta;
    lightboxDesc.textContent = data.desc;
    
    // Set Goods (SUZURI) link visibility
    if (linkGoods) {
        if (data.suzuriUrl && data.suzuriUrl !== '#') {
            linkGoods.href = data.suzuriUrl;
            linkGoods.style.display = 'flex';
        } else {
            linkGoods.style.display = 'none';
        }
    }
    
    // Set Twitter share action dynamically
    if (btnShare) {
        btnShare.onclick = () => {
            const text = encodeURIComponent(`rai個展「Muted Whispers」で『${data.title}』を鑑賞しています。`);
            const shareUrl = `https://twitter.com/intent/tweet?text=${text}&url=${encodeURIComponent(window.location.origin + window.location.pathname)}&hashtags=MutedWhispers,rai_gallery`;
            window.open(shareUrl, '_blank');
        };
    }
    
    // Set dynamic aura background color
    if (lightboxAura && data.auraColor) {
        lightboxAura.style.setProperty('--aura-color', data.auraColor);
    }
    
    lightbox.classList.remove('hide');
    lightbox.offsetWidth;
    lightbox.classList.add('show');
    
    triggerBleedEffect();
}

function closeLightbox() {
    lightbox.classList.remove('show');
    if (bleedAnimId) {
        cancelAnimationFrame(bleedAnimId);
        bleedAnimId = null;
    }
    setTimeout(() => {
        lightbox.classList.add('hide');
        if (bleedDisplacement) {
            bleedDisplacement.setAttribute('scale', 0);
        }
    }, 500);
}

// Lightbox Navigation
function navigateArtwork(direction) {
    let currentIndex = artworkKeys.indexOf(currentArtworkKey);
    if (direction === 'next') {
        currentIndex = (currentIndex + 1) % artworkKeys.length;
    } else {
        currentIndex = (currentIndex - 1 + artworkKeys.length) % artworkKeys.length;
    }
    
    const newKey = artworkKeys[currentIndex];
    
    lightboxImg.style.opacity = '0';
    lightboxTitle.style.opacity = '0';
    lightboxMeta.style.opacity = '0';
    lightboxDesc.style.opacity = '0';
    
    setTimeout(() => {
        openLightbox(newKey);
        lightboxImg.style.opacity = '1';
        lightboxTitle.style.opacity = '1';
        lightboxMeta.style.opacity = '1';
        lightboxDesc.style.opacity = '1';
    }, 300);
}

btnPrev.addEventListener('click', () => navigateArtwork('prev'));
btnNext.addEventListener('click', () => navigateArtwork('next'));

// Keyboard accessibility for lightbox
document.addEventListener('keydown', (e) => {
    if (lightbox.classList.contains('show')) {
        if (e.key === 'Escape') {
            closeLightbox();
        } else if (e.key === 'ArrowRight') {
            navigateArtwork('next');
        } else if (e.key === 'ArrowLeft') {
            navigateArtwork('prev');
        }
    }
});

// Close lightbox on click outside the content box
lightbox.addEventListener('click', (e) => {
    if (e.target === lightbox) {
        closeLightbox();
    }
});

// 8. Dynamic zoning: backdrop colors & synth morphing logic
function hexToRgb(hex) {
    const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    return result ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16)
    } : null;
}

function lerp(start, end, amt) {
    return Math.round((1 - amt) * start + amt * end);
}

function startStarChimeLoop() {
    if (starChimeInterval) clearInterval(starChimeInterval);
    
    starChimeInterval = setInterval(() => {
        if (!audioCtx || audioCtx.state === 'suspended' || !isPlaying) return;
        
        if (Math.random() > 0.45) {
            const scale = [783.99, 880.00, 1046.50, 1174.66, 1318.51, 1567.98];
            const randomFreq = scale[Math.floor(Math.random() * scale.length)];
            
            const osc = audioCtx.createOscillator();
            const gainNode = audioCtx.createGain();
            
            osc.type = 'sine';
            osc.frequency.setValueAtTime(randomFreq, audioCtx.currentTime);
            osc.detune.setValueAtTime((Math.random() - 0.5) * 15, audioCtx.currentTime);
            
            const now = audioCtx.currentTime;
            gainNode.gain.setValueAtTime(0, now);
            gainNode.gain.linearRampToValueAtTime(0.02 + Math.random() * 0.015, now + 1.2);
            gainNode.gain.exponentialRampToValueAtTime(0.0001, now + 3.8);
            
            osc.connect(gainNode);
            gainNode.connect(starChimeGain);
            
            osc.start(now);
            osc.stop(now + 3.9);
        }
    }, 1500);
}

function updateAmbientSynth(scrollLeft, maxScroll) {
    if (!audioCtx || !isPlaying) return;
    
    const ratio = maxScroll > 0 ? scrollLeft / maxScroll : 0;
    
    const minFreq = 170;
    const maxFreq = 480;
    const targetFreq = minFreq + (maxFreq - minFreq) * ratio;
    
    if (mainFilterNode) {
        mainFilterNode.frequency.setTargetAtTime(targetFreq, audioCtx.currentTime, 0.4);
    }
    
    if (starChimeGain) {
        let chimeVol = 0;
        if (ratio > 0.5) {
            chimeVol = (ratio - 0.5) * 2;
        }
        starChimeGain.gain.setTargetAtTime(chimeVol * 0.5, audioCtx.currentTime, 0.4);
    }
}

// Perform 3D parallax on scroll
function updateParallax() {
    const walls = document.querySelectorAll('.dynamic-wall');
    const containerWidth = galleryContainer.clientWidth;
    const scrollLeft = galleryContainer.scrollLeft;
    
    walls.forEach(wall => {
        const wallStart = wall.offsetLeft;
        const wallWidth = wall.offsetWidth;
        const wallCenter = wallStart + wallWidth / 2;
        const scrollCenter = scrollLeft + containerWidth / 2;
        
        // Relative distance from viewport center (-1.0 to 1.0)
        const distance = (scrollCenter - wallCenter) / (containerWidth / 2);
        
        if (Math.abs(distance) < 2) {
            const midLayer = wall.querySelector('.mid-layer');
            const foreLayer = wall.querySelector('.fore-layer');
            
            if (midLayer) {
                // Move midground slower (opposite to scroll direction)
                const midX = distance * -30; 
                midLayer.style.setProperty('--mid-x', `${midX}px`);
            }
            if (foreLayer) {
                // Move foreground faster (in scroll direction)
                const foreX = distance * 40;
                foreLayer.style.setProperty('--fore-x', `${foreX}px`);
            }
        }
    });
}

// Handle room changes via ink overlay transitions
function checkRoomTransitions(scrollLeft) {
    if (isTransitioning) return;
    
    const overlay = document.getElementById('room-transition-overlay');
    if (!overlay) return;
    
    let targetZone = '';
    let targetScroll = 0;
    let transitionColor = 'rgba(144, 210, 220, 0.95)'; // water -> forest (greenish blue)
    
    if (currentZone === 'water' && scrollLeft > zoneLimits['water'] && zoneLimits['water'] > 0) {
        targetZone = 'forest';
        targetScroll = zoneLimits['water'] + 250;
        transitionColor = 'rgba(144, 210, 220, 0.95)';
    } else if (currentZone === 'forest' && scrollLeft > zoneLimits['forest'] && zoneLimits['forest'] > 0) {
        targetZone = 'stars';
        targetScroll = zoneLimits['forest'] + 250;
        transitionColor = 'rgba(220, 180, 240, 0.95)'; // forest -> stars (purple/magenta)
    } else if (currentZone === 'forest' && scrollLeft < zoneLimits['water'] && zoneLimits['water'] > 0) {
        targetZone = 'water';
        targetScroll = zoneLimits['water'] - 200;
        transitionColor = 'rgba(144, 210, 220, 0.95)';
    } else if (currentZone === 'stars' && scrollLeft < zoneLimits['forest'] && zoneLimits['forest'] > 0) {
        targetZone = 'forest';
        targetScroll = zoneLimits['forest'] - 200;
        transitionColor = 'rgba(220, 180, 240, 0.95)';
    }
    
    if (targetZone) {
        isTransitioning = true;
        currentZone = targetZone;
        
        // Trigger overlay ink stain bleed animation
        overlay.style.setProperty('--transition-color', transitionColor);
        overlay.classList.add('show-transition');
        
        setTimeout(() => {
            // Jump container scroll inside the ink stain
            galleryContainer.scrollTo({
                left: targetScroll,
                behavior: 'auto'
            });
            updateBackdropAndSound();
        }, 800);
        
        setTimeout(() => {
            overlay.classList.remove('show-transition');
            isTransitioning = false;
        }, 1800);
    }
}

function updateBackdropAndSound() {
    const sections = Array.from(document.querySelectorAll('.gallery-section'));
    if (sections.length === 0) return;
    
    const containerWidth = galleryContainer.clientWidth;
    const scrollLeft = galleryContainer.scrollLeft;
    const scrollCenter = scrollLeft + containerWidth / 2;
    
    const isNight = document.body.classList.contains('theme-night');
    const colorAttr = isNight ? 'data-bg-night' : 'data-bg-day';
    
    let activeIndex = 0;
    for (let i = 0; i < sections.length; i++) {
        const sec = sections[i];
        const secStart = sec.offsetLeft;
        const secEnd = secStart + sec.offsetWidth;
        
        if (scrollCenter >= secStart && scrollCenter <= secEnd) {
            activeIndex = i;
            break;
        }
        if (scrollCenter < secStart) {
            activeIndex = Math.max(0, i - 1);
            break;
        }
        if (i === sections.length - 1) {
            activeIndex = i;
        }
    }
    
    let nextIndex = activeIndex;
    let blendRatio = 0;
    
    const currentSec = sections[activeIndex];
    const currentCenter = currentSec.offsetLeft + currentSec.offsetWidth / 2;
    
    if (scrollCenter > currentCenter && activeIndex < sections.length - 1) {
        nextIndex = activeIndex + 1;
        const nextSec = sections[nextIndex];
        const nextCenter = nextSec.offsetLeft + nextSec.offsetWidth / 2;
        blendRatio = (scrollCenter - currentCenter) / (nextCenter - currentCenter);
    } else if (scrollCenter < currentCenter && activeIndex > 0) {
        nextIndex = activeIndex - 1;
        const prevSec = sections[nextIndex];
        const prevCenter = prevSec.offsetLeft + prevSec.offsetWidth / 2;
        blendRatio = (currentCenter - scrollCenter) / (currentCenter - prevCenter);
        
        const temp = activeIndex;
        activeIndex = nextIndex;
        nextIndex = temp;
        blendRatio = 1 - blendRatio;
    }
    
    blendRatio = Math.max(0, Math.min(1, blendRatio));
    
    const hexStart = sections[activeIndex].getAttribute(colorAttr) || '#0f1113';
    const hexEnd = sections[nextIndex].getAttribute(colorAttr) || '#0f1113';
    
    const rgbStart = hexToRgb(hexStart);
    const rgbEnd = hexToRgb(hexEnd);
    
    if (rgbStart && rgbEnd) {
        const r = lerp(rgbStart.r, rgbEnd.r, blendRatio);
        const g = lerp(rgbStart.g, rgbEnd.g, blendRatio);
        const b = lerp(rgbStart.b, rgbEnd.b, blendRatio);
        
        const blendedColor = `rgb(${r}, ${g}, ${b})`;
        galleryContainer.style.backgroundColor = blendedColor;
        document.documentElement.style.setProperty('--wall-color', blendedColor);
    }
    
    const maxScroll = galleryContainer.scrollWidth - containerWidth;
    updateAmbientSynth(scrollLeft, maxScroll);
    updateParallax();
    checkRoomTransitions(scrollLeft);
}

// Bind scrolling and resizing events to trigger updates
galleryContainer.addEventListener('scroll', updateBackdropAndSound);
window.addEventListener('resize', () => {
    updateBackdropAndSound();
    calculateZoneBoundaries();
});

// Loader Control Simulation
function initLoader() {
    const loader = document.getElementById('loader');
    const loaderBar = document.getElementById('loader-bar');
    const loaderStatus = document.querySelector('.loader-status');
    if (!loader || !loaderBar) return;
    
    let progress = 0;
    const interval = setInterval(() => {
        progress += Math.random() * 15;
        if (progress >= 100) {
            progress = 100;
            clearInterval(interval);
            
            loaderBar.style.width = '100%';
            if (loaderStatus) loaderStatus.textContent = '空間の準備が整いました。';
            
            setTimeout(() => {
                loader.classList.add('fade-out');
                setTimeout(() => loader.style.display = 'none', 1500);
            }, 600);
        } else {
            loaderBar.style.width = `${progress}%`;
            if (progress > 60 && loaderStatus) {
                loaderStatus.textContent = '紙に絵の具を溶かしています...';
            } else if (progress > 30 && loaderStatus) {
                loaderStatus.textContent = '光のささやきを集めています...';
            }
        }
    }, 150);
}

// Expose functions globally for inline HTML click handlers (due to ES module scope)
window.openLightbox = openLightbox;
window.closeLightbox = closeLightbox;
window.navigateArtwork = navigateArtwork;

// Safely generate gallery and launch loader when DOM is fully prepared
window.addEventListener('DOMContentLoaded', () => {
    generateGallery();
    initLoader();
});
